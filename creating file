#include<stdio.h>
int main(){
    FILE *fp;
    fp=fopen("sample.txt","w");
    if(fp==NULL){
        printf("File cannot be opened");
        return 1;
    }
    printf("File is created successfully");
    fclose(fp);
    return 0;
}
