#include<stdio.h>
void main(){
    FILE *fp=fopen("data.txt","w");
    if(fp==NULL) {
        printf("File not found");
    }
    fprintf(fp,"Hi\n");
    fprintf(fp,"I am Moulika");
    printf("Data is added successfully into the file");
    fclose(fp);
}
