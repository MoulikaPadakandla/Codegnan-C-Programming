#include<stdio.h>
int main(){
    char src[] = "hello",dest[20];
    char *p=src,*q=dest;
    while(*p!='\0'){
        *q=*p;
        p++,q++;
    }
    *q='\0';
    printf("Copied data is %s",dest);
    return 0;
}
