#include <stdio.h>
#include <stdlib.h>
int main()
{
    int *ptr;
    ptr = (int *)malloc(sizeof(int));
    printf("Enter a number: ");
    scanf("%d", ptr);
    printf("Number = %d\n", *ptr);
    free(ptr);
    printf("Memory has been freed.");
    return 0;
}
